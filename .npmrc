# React Native / Expo require a flat node_modules layout (Metro cannot resolve
# pnpm's nested symlinked .pnpm store reliably). Force hoisting.
node-linker=hoisted

# FIX (DAT-4.9): Use frozen lockfile for deterministic CI/production installs.
# Set to false only during active development when lockfile changes are expected.
prefer-frozen-lockfile=true