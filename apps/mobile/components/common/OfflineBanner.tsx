/**
 * BuildFlow - Universal Offline Status & Sync Banner.
 * Renders the stateful OfflineSyncBanner for offline detection, sync progress & completion.
 */
import React from 'react';
import { OfflineSyncBanner } from './OfflineSyncBanner';

export function OfflineBanner() {
  return <OfflineSyncBanner />;
}
