/**
 * BuildFlow - Permissions barrel
 *
 * Usage:
 *   import { PERMISSIONS, type Permission } from '@buildflow/shared/permissions';
 *   import { DEFAULT_ROLE_PERMISSIONS } from '@buildflow/shared';
 */
export * from './catalog';
export * from './defaults';